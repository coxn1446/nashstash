runtime: nodejs20
service: qa-service
instance_class: F2

env_variables:
  NODE_ENV: qa

automatic_scaling:
  min_instances: 0
  max_instances: 5
  target_cpu_utilization: 0.6

handlers:
  - url: /.*
    script: auto

